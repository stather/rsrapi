[{% for a in model.animations %}
    {"name":"{{ a.name }}",
    "thumb":"{{ a.thumbNailUrl }}",
    "atlas":"{{ a.atlasUrl }}",
    "texture":"{{ a.textureUrl }}",
    "rewardImage":"{{ a.rewardImage }}",
    "json":"{{ a.jsonUrl }}" },
{% endfor %}
]